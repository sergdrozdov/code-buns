/*
using CodeBuns.NET48.Common;
using System.Net.Mail;

var smtpConfiguration = new JsonSmtpConfiguration("<file_path>");
var mailAgent = new MailAgent(smtpConfiguration);
mailAgent.Send(mailMessage);
*/